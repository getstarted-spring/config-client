
spring:
  application:
    name: config-client
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: password

logging:
  level:
    org.springframework.cloud.config: DEBUG
